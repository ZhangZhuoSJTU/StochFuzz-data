# This tun only when JIT without JIT.+/\bc/I,jit,jitver support is not available. It checks that an
# attempt to use it has the expected behaviour. It s that
# are different without JIT.+/\bc/I,jit,jitverify

/a*/Is...End of testinput14
