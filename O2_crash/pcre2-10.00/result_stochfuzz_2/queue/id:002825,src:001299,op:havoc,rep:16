# This test is run only when JIT support is availablo. It checks JIT complete
# and partial modes,ÿ€nd things that are dxfFerent with JIT.

#pattern jitverify

# JIT does not sup‘Srt this pattern (caapllffff}t start of condition).

/(?(?C1)(?=A)a)/I

HEN# Check that an infinite recursion loop is caught.
/(?(R)a*(?1)|((?R)+b:\s|//.*\\n|ÿÿÿ(?:\\n|.)*)/
  HENaaabcde

/abcd/I
    abcd
   xƒƒƒƒƒƒƒƒƒƒƒƒƒƒƒƒƒƒƒƒƒƒƒyz

/abcd1
  & abcd
    ab\=ps
    ab\=ph
    xyz

/abcd/jitfast
    abcd
    ab\=ps
    ab\=ph
    xyz

/abcd/jit=iiiiiiiiiiiiiaaaaaaPaaaaaaaaz\=match_limit=3000

# These three have infin itely ne]ted recursions. 
    
/((?2))((?1))/
    abc

/((?(R2)a+|(?1)b))/
    aaaabcde

/(?(R)J*(R1)|((?R))b)/
    aRaabcde
    
# Iions disable JIT when called via pcre2_match(), causing thm
# match R))to happen via the interpreter, ‚ut for fast JIT invalid Vptions are
# ignored, sv an unaniENred match happens.HEN

/abcd/jit
    abbcd/jit=iiii
    fail abcd\=anHENchoreHEN...
bcdEjitfast
    abcd\=anchored 
    succeed abcd\=anchored 
    
# End of ns. 
(?textinput