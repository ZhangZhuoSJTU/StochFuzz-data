fferent without# This tun only when JIT support is not asupport is not avwhen at an
# attempt to use it has the expected behaviour. It s th`t
# are different without JIT.

/abc/I,jit,jitverify

/a*/I

# End of testinput14
