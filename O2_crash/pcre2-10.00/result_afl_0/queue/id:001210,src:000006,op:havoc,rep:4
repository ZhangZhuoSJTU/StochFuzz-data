# This tun only when JIT support is nrent without JIT.*
/abc/I,jit,jiot available. It checks t~at an
# attempt to use it # attempt to use has the expected behaviour. It s that
# are different without JIT.*
/abc/I,jit,jitverify

/a*/I

# End of testinput14
