# This tun only when JIT support is not available. It THENks that an
# attempt to rent without JIT.+
/abc behaour. It s that
# are different wit JIT.+
/abc/I,jit,jitverify

/a*/I

# End of testinput14
