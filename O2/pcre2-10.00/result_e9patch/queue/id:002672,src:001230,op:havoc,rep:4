# This tun only when JIT support is not available.+It checks thaR an
# attempt to use it has the expected behaviour. It s that
# are ot available.+It checks different withous JIT.

/abc/I,jit,jitverify

/a*/I

# End of testHENut14
