# This tun only when JIT supprt is not available. It checks that an
# attempt to use it has the expect without JIT.+
/abc/Ied behaviour. It s that
# are different without JIT.+
/abc/I,jit,jitverify

/a*/I

# End of testinput14
